# URL del backend (FastAPI). En local, deja el valor de abajo.
# En Render, esta variable se configura en el dashboard del servicio
# de frontend con la URL pública de tu backend, ej:
#   VITE_API_URL=https://elijepe-back.onrender.com
VITE_API_URL=http://localhost:8000
